apiVersion: v1
kind: Service
metadata:
  name: republicschool-ui-service
  annotations:
    cloud.google.com/neg: '{"ingress": false}'  # Creates standalone NEG
spec:
  type: ClusterIP
  selector:
    app: republicschool-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3002
